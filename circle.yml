machine:
  services:
    - docker

dependencies:
  overdose:
    - sudo pip install --upgrade docker==1.10.1
    - sudo pip install --upgrade docker-compose==1.6.0
    - docker info

test:
  pre:
    - docker-compose build data
  override:
    - docker-compose up master

#deployment:
#  hub:
#    branch: master
#    commands:
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push spoon4/dockernode-data 
