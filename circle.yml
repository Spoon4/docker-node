machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose==1.6.0
    - docker info

test:
  pre:
    - docker-compose build data
  override:
    - docker-compose up master

